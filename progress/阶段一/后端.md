# 阶段一后端进度

## 已完成

- Laravel 10 后端脚手架与 Podman 本地运行环境初始化完成。
- 核心数据结构已落地：`users` 增强、`roles`、`menus`、`user_roles`、`user_projects`、`role_menu_permissions`、`audit_logs`。
- Sanctum Bearer Token 认证链路已打通。
- 认证接口已完成：
  - `POST /api/v1/auth/login`
  - `POST /api/v1/auth/logout`
  - `POST /api/v1/auth/change-password`
  - `GET /api/v1/auth/me`
  - `GET /api/v1/auth/menus`
- 用户管理接口已完成：
  - `GET /api/v1/admin/users`
  - `GET /api/v1/admin/users/{id}`
  - `POST /api/v1/admin/users`
  - `PUT /api/v1/admin/users/{id}`
  - `PATCH /api/v1/admin/users/{id}/status`
  - `PATCH /api/v1/admin/users/{id}/unlock`
  - `POST /api/v1/admin/users/{id}/reset-password`
- 角色管理接口已完成：
  - `GET /api/v1/admin/roles`
  - `GET /api/v1/admin/roles/{id}`
  - `POST /api/v1/admin/roles`
  - `PUT /api/v1/admin/roles/{id}`
  - `POST /api/v1/admin/roles/{id}/copy`
  - `PATCH /api/v1/admin/roles/{id}/status`
  - `GET /api/v1/admin/roles/{id}/permissions`
  - `PUT /api/v1/admin/roles/{id}/permissions`
- 菜单管理接口已完成：
  - `GET /api/v1/admin/menus/tree`
  - `POST /api/v1/admin/menus`
  - `PUT /api/v1/admin/menus/{id}`
  - `PATCH /api/v1/admin/menus/{id}/status`
  - `DELETE /api/v1/admin/menus/{id}`
- 审计日志查询接口已完成：
  - `GET /api/v1/admin/audit-logs`
  - `GET /api/v1/admin/audit-logs/{id}`
- 接口级权限中间件、只读角色写操作限制、系统管理员不可降权兜底已完成。
- 认证链路的审计日志写入已补齐到登出成功、锁定到期自动解锁等关键场景，并已补充契约测试覆盖。
- 异常码与错误语义已按接口基线统一到 `1001/1002/1003/1004/1005/5000`，并补充未登录、无权限、资源不存在、参数校验失败等回归测试。
- 用户、角色、菜单、认证相关接口的关键成功文案、状态标签、组织名称与审计日志标签已对齐后端返回契约。
- 角色详情接口已新增 `active_user_count` 与 `disabled_user_count`，用于区分角色下启用用户数与停用用户数，且不影响原有 `user_count` 口径。

## 待完成

- 审计日志写入覆盖继续补齐到剩余关键后台管理动作。
- 补充更多 Feature Test、权限测试与基础回归测试。

## 阻塞

- 无硬阻塞，当前主要是后端收口与测试补齐工作量较大。
