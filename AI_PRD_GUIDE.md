# AI 引导文件（固定模板）：初始化项目结构与文档

## 人类开发落地总体流程（给人类看的，AI 可忽略）

> 本节用于指导**人类**如何落地一个项目的完整闭环；AI 可以忽略本节内容（不作为模板生成约束）。

### 1）需求调研阶段
- 根据需求进行调研与澄清，识别边界与优先级。
- 检验可实现的技术方案，确认关键风险点与约束（性能、安全、部署、成本等）。
- 最终确定技术架构与技术栈，并沉淀到需求文档/架构说明中。

### 2）开发阶段（前后端协作）
- **前端**：
  - 开发前端时使用 MCP 与 `ui/` 对接，按高还原度实现设计稿。
  - 组件与布局遵循统一规范，避免页面间不一致。
- **后端**：
  - 后端生成/维护 API 文档（接口路径、权限点、请求/响应字段、错误码等）。
  - 与前端对接接口契约，确保联调一致性。

### 3）测试阶段
- 测试通过后再进入下一步交付。
- 测试发现问题：提交 issue，并推动前后端修复与回归（保证闭环）。

### 4）需求变更（版本化）
- 确定需求变更后：**保留原需求文档**。
- 生成新的需求文档版本（例如 `PRODUCT_REQUIREMENTS_v2.md` 或 `PRODUCT_REQUIREMENTS.md` + 版本章节/变更记录）。
- 让 AI 对比 v1/v2 差异，输出差异清单与影响分析（页面、接口、数据、权限、测试用例等）。

### 5）人类/AI 交替开发协作
- 人类本地开发为主。
- 人类休息时：让 OpenClaw 拉取最新代码，基于 `memory/`、`progress/`、`issue/`、`ui/` 等上下文继续开发与完善。

## 0. 目的（必须理解）
本文件是**本仓库的固定模板规范**：用于在新项目中**原样生成**一套可被 AI/开发者接续的目录结构与初始化文件。

关键要求：
- **初始化内容必须固定**：按本文件给出的“标准内容”逐字复制生成；**不得**由 AI 自行推理、自由发挥或改写。
- 若需要变更模板内容，应先更新本仓库模板文件，再同步更新本引导文件中的标准内容。

---

## 1. 初始化后必须存在的目录与文件（固定清单）
AI 读完本文件后，必须在仓库里生成以下结构（路径固定，内容固定；“标准内容”见第 2 节）：  

- `.gitignore`（忽略依赖目录与本地私密配置，内容固定见 2.0 节）
- `PRODUCT_REQUIREMENTS.md`
- `rule.md`
- `AI_PRD_GUIDE.md`（本文件）
- `changelog/`（迭代记录目录，格式模仿 `issue/`：每条迭代一个文件，记录迭代日期与迭代内容）
  - `changelog/README.md`
- `memory/`
  - `memory/2026-03-18.md`（模板示例记忆；后续按日期新增）
- `progress/`
  - `progress/PHASES.md`
  - `progress/CURRENT_PROGRESS.md`
- `app/`
  - `app/README.md`（占位说明；后续所有代码/产物均在 app 下）
- `issue/`
  - `issue/README.md`（占位说明；issue 文件按 `rule.md` 约定）
- `ui/`
  - `ui/README.md`（占位说明；UI 设计数据放这里）
- `config/`（本地私密配置目录）
  - `config/.gitkeep`（空占位；用于确保目录存在）
- `tests/`
  - `tests/README.md`（占位说明；测试用例放这里）

---

## 2. 初始化文件的标准内容（必须原样复制）

### 2.0 `.gitignore`
```gitignore
# 依赖目录
node_modules/
.pnp
.pnp.js
vendor/

# 本地私密配置（令牌、密钥等个人配置，不提交仓库）
config/

# 构建产物
.next/
out/
dist/
build/

# 系统文件
.DS_Store
Thumbs.db
```

### 2.1 `changelog/README.md`
```md
# changelog/

本目录为**迭代记录**目录，格式与 `issue/` 类似：每条迭代一个文件。

- **命名建议**：`YYYY-MM-DD-简短描述.md` 或 `001-YYYY-MM-DD.md`（由项目约定）。
- **内容需包含**：迭代日期、迭代内容（版本变更/发布说明/完成项等）；可选：版本号、关联 issue。
- 当开发者要求增加迭代记录时，将新记录放在本目录下（见 `rule.md`）。
```

#### 2.1.1 初始化时可选：首条迭代记录示例 `changelog/YYYY-MM-DD-初始化项目结构与文档.md`
```md
# 迭代记录 YYYY-MM-DD：初始化项目结构与文档

- **迭代日期**：（填写日期）
- **迭代内容**：
  - 初始化项目结构与文档模板（PRD、引导文件、人格文件、进度与记忆目录等）。
```

### 2.2 `app/README.md`
```md
# app/

本目录用于存放**生产代码与产品产物**（按 `rule.md` 的约定）。

- 在此放置可直接运行的应用代码（例如前端页面、后端 API、组件、样式、配置等）。
- 具体项目结构以 `PRODUCT_REQUIREMENTS.md` 为准。
```

### 2.3 `issue/README.md`
```md
# issue/

本目录用于存放项目 Issue（需求/缺陷/任务）。

- 文件格式与命名规则以 `rule.md` 与项目约定为准。
- 建议每个 issue 单独一个文件，并包含：标题、描述、类型、（可选）优先级、（可选）验收标准。
```

### 2.4 `ui/README.md`
```md
# ui/

本目录用于存放 UI 相关文件与说明。

- 例如：设计稿、原型说明、切图/图标资源、设计 token 导出等。
- 若提供了 MCP/Figma 链接或接入方式，应在此目录内明确说明，供开发时高还原实现。
```

### 2.4.1 线上方案时：`issue/README.md` 与 `ui/README.md` 作为 skill
- 当 `issue/` 或 `ui/` 使用线上方案（如 MCP、Figma、在线系统）时：
  - `issue/README.md` 与 `ui/README.md` 必须写成可执行的 skill 指南，明确如何连接、如何读取、如何使用。
  - README 只写参数名与接入步骤，不写死个人令牌。
- 各开发者的令牌/密钥等私密信息统一放本地 `config/` 目录（例如 `config/issue.local.env`、`config/ui.local.env`），不写入模板正文。
- AI 读取 README 后应按其中步骤接入；若缺少参数或令牌，先向开发者询问，不自行猜测。

### 2.5 `tests/README.md`
```md
# tests/

本目录用于存放测试用例与测试代码（如单测/E2E）。

- 测试用例建议用 Markdown 描述：场景、步骤、预期结果。
- 自动化测试工具与目录结构以项目约定为准，并在 `PRODUCT_REQUIREMENTS.md` 或 README 中说明。
```

### 2.6 `progress/PHASES.md`
```md
# 项目阶段与任务拆分

## 说明
本文件按阶段拆分项目任务，用于规划与跟踪。每个阶段列出目标：完成哪些页面或功能。具体子任务与验收标准可在各阶段下补充，或与 `issue/` 关联。

---

## 阶段一
- **目标**：（在此填写，例如：登录与布局、用户管理列表与 CRUD、基础权限与菜单）
- **交付物**：
  - 页面/功能 1
  - 页面/功能 2
  - …

---

## 阶段二
- **目标**：（在此填写）
- **交付物**：
  - 页面/功能 1
  - 页面/功能 2
  - …

---

## 阶段三及以后
- 按项目实际拆分追加阶段。

---

*与 `progress/CURRENT_PROGRESS.md` 配合使用：本文件定义「要做哪些」，当前进度文件记录「做到哪了、有什么问题」。*
```

### 2.7 `progress/CURRENT_PROGRESS.md`
```md
# 当前进度跟踪

**当前阶段**：阶段一（开发时仅允许做本阶段及之前阶段，严禁做本阶段之后的内容；修改阶段时请同步更新此处。）

## 说明
本文件记录相对 `progress/PHASES.md` 的当前状态：各阶段已完成项、待完成项、存在问题。每次有实质进展或阻塞时更新，便于你或 AI 接续时快速了解现状。

---

## 阶段一

| 状态     | 项 |
|----------|----|
| 已完成   | （列举已完成页面/功能） |
| 待完成   | （列举未完成项） |
| 存在问题 | （列举阻塞、Bug、待决策项；可附 issue 编号或链接） |

---

## 阶段二

| 状态     | 项 |
|----------|----|
| 已完成   | |
| 待完成   | |
| 存在问题 | |

---

## 阶段三及以后
- 按实际阶段追加表格或列表。

---

*更新时请注明日期（可选），例如：2026-03-18 更新。*
```

### 2.8 `rule.md`
```md
# AI 写码人格与规则（每次写代码必须遵守）

## 使用方式
任何 AI 在为本项目编写或修改代码前，必须先阅读本文件，并在整个会话中遵守以下规则。

---

## 1. 前后端联动与影响范围（必须执行）

### 1.1 修改前端代码时
- **必须**思考并主动检查：
  - 对**其他页面/组件**的影响：是否共用组件、共享状态、路由或布局；是否有引用处需要同步修改。
  - 对**后端**的影响：请求的 URL、方法、请求体/查询参数、响应结构是否与现有 API 一致；若前端期望变更接口契约，是否已与后端实现或 API 文档对齐，并同步修改后端或接口约定。
- 修改后若涉及接口形状变更，须在 API 层/后端做对应更新或记录在 `issue/`，避免前后端不一致。

### 1.2 修改后端代码时
- **必须**思考并主动检查：
  - 对**前端**的影响：接口路径、方法、请求/响应字段是否被前端使用；若有破坏性变更，须同步修改前端调用处及类型定义，或注明兼容方案与迁移步骤。
  - 对**其他接口或服务**的影响：是否被其他 API、定时任务或内部调用依赖；数据模型或权限逻辑变更是否影响现有功能。
- 修改后若涉及接口或数据契约变更，须同步更新前端调用与类型，或在 `progress/` / `issue/` 中记录待办。

### 1.3 建议动作
- 修改前可快速搜索/检索：相关 API 路径、组件名、类型定义、共享常量，再动手改。
- 一次改动若横跨前后端，尽量在同一轮对话中完成前后端一致修改，或明确列出未完成项并写入 `progress/CURRENT_PROGRESS.md` 的「存在问题」或 `issue/`。

---

## 2. 遵守进度：仅开发当前阶段及以下（必须执行）

- 开发时**必须**以 `progress/PHASES.md` 与 `progress/CURRENT_PROGRESS.md` 为准，**只开发或完善「当前进度」所在阶段及其之前阶段**的代码。
- 例如：若当前进度为**阶段二**，则只允许根据 `PHASES.md` 开发或完善**阶段二**和**阶段一**相关功能与代码；**不得**主动开发阶段三及以后在 `PHASES.md` 中定义的内容。
- 若用户明确要求提前做后续阶段，再按用户指示执行；否则一律以进度文件中的当前阶段为上限。

---

## 3. UI 还原：以 ui 目录与设计数据为准（必须执行）

- 开发界面代码时**必须**查看 `ui/` 目录，以**高还原度**为准实现。
- 若 `ui/` 下提供了 **MCP** 或 **Figma** 的链接/接入方式，**优先以这些数据为准**（尺寸、间距、颜色、组件结构等），再落成页面与组件代码。
- 未提供 MCP 或 Figma 时，以 `ui/` 内的设计稿、说明或 token 导出为准，保证实现与设计一致。

---

## 4. 产出位置：代码与产品产物均在 app 目录下

- 本项目的**代码**与**产品产物**均生成在 **`app/`** 目录下。
- 前端页面、后端 API、组件、样式、配置等应用相关产出一律放在 `app/` 或其子目录中，不散落在项目根目录或其他约定外的位置。

---

## 5. 开发完毕检查 issue 目录（必须执行）

- **每次开发完毕后**，检查 `issue/` 目录。
- `issue/` 下的文件使用 **Word 文档**（`.docx`）；每个 issue 的状态通过**文件名或文档内标注**体现，取值为：**已解决**、**未解决**、**待定**（例如文件名：`已解决-xxx.docx` / `未解决-xxx.docx` / `待定-xxx.docx`）。
- 检查时**忽略**状态为「已解决」的 issue，**仅针对「未解决」**的 issue 逐一向开发者询问：该 issue 是否已经解决？
- 若开发者回复表示已解决，则**将状态更新为「已解决」**（如通过重命名文件为 `已解决-原标题.docx` 或在文档内更新状态，以项目约定为准）。

---

## 6. 其他通用规则（可随项目补充）

- 遵守 `PRODUCT_REQUIREMENTS.md` 与 `AI_PRD_GUIDE.md` 中的技术选型、目录约定与产出要求。
- 关键逻辑（鉴权、权限、审计日志、支付等）须有中文注释说明意图与风险点。
- 不提交敏感信息（密码、密钥、Token）进仓库；配置通过环境变量或 `.env`（且 `.env` 不提交）。
- 若发现需求歧义或实现冲突，先与用户确认，不擅自二选一。

---

*本项目可根据需要在本文件中继续追加规则，AI 以最新内容为准。*
```

### 2.9 `memory/2026-03-18.md`
初始化时必须创建 `memory/` 目录，并放入本模板的示例记忆文件（内容固定，原样复制）。该文件较长，直接以本仓库现有文件内容为准复制即可。

### 2.10 `PRODUCT_REQUIREMENTS.md`
初始化时必须创建 `PRODUCT_REQUIREMENTS.md`。该文件内容以本仓库现有文件为准，**原样复制**（不允许 AI 自行改写或“生成一份类似的”）。

### 2.11 `config/.gitkeep`
```txt
# empty placeholder
```

---

## 3. 执行顺序（固定）
1) 生成第 1 节中的全部目录与文件。
2) 用第 2 节中的“标准内容”填充对应文件（逐字复制）。
3) 之后才允许进入需求澄清、PRD 编写与编码阶段；任何变更通过更新模板文件与 memory 记录进行追踪。
