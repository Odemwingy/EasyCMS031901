# 2026-03-23 完成用户角色菜单权限基础接口联调

## 变更内容

- 完成用户管理接口的创建、详情、更新、状态切换、重置密码、解锁，并完成实际联调验证。
- 用户列表接口补充分页元信息与标签字段，兼容前端 `per_page/current_page/last_page` 结构。
- 新增角色管理接口：
  - `GET /api/v1/admin/roles`
  - `POST /api/v1/admin/roles`
  - `POST /api/v1/admin/roles/{id}/copy`
  - `PATCH /api/v1/admin/roles/{id}/status`
  - `GET /api/v1/admin/roles/{id}/permissions`
  - `PUT /api/v1/admin/roles/{id}/permissions`
- 新增菜单树接口：
  - `GET /api/v1/admin/menus/tree`
- 新增按钮级菜单/权限种子数据，覆盖用户管理、角色管理、权限配置等场景。
- 修复 `UpdateUserRequest` 缺少 `Rule` 导入导致的更新接口 500。
- 修复多处接口乱码提示，并增加 API 统一 500 JSON 返回。

## 验证结果

- 已通过本地 Podman 环境实际验证：
  - 登录
  - 用户列表/详情/创建/更新/停用/重置密码/解锁
  - 角色列表/创建/复制/停用
  - 菜单树读取
  - 角色权限读取与更新

## 备注

- 当前后端已具备与现有前端后台管理主页面进行基础联调的接口能力。
- 审计日志查询接口、角色详情页、更细的菜单管理 CRUD 仍待后续继续开发。
