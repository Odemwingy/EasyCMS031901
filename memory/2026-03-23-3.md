# 记忆 2026-03-23-3

## 变更类型
环境初始化 + 后端脚手架

## 变更摘要
完成阶段一本地后端运行环境初始化，在 `app/backend/` 落地 Laravel 10 工程，并以 Podman 本地镜像跑通 PHP、Nginx、MySQL、Redis。

## 变更详情
- 在 `app/backend/` 初始化 Laravel 10 工程，生成 `artisan`、`composer.json`、`routes/`、`config/`、`database/` 等基础结构。
- 新增 `app/backend/docker-compose.yml`，定义本地后端容器编排。
- 新增 `app/backend/docker/nginx/default.conf` 与 `app/backend/docker/php/local.ini`。
- 本地运行镜像固定为：
  - PHP：`096d3353d349`
  - MySQL：`3218b38490ce`
  - Redis：`7614ae9453d1`
  - Nginx：`ad5708199ec7`
- 本地端口固定为：
  - MySQL：`7050`
  - Nginx/API：`7051`
  - Redis：`7052`
- 为解决 Windows 挂载目录下 Laravel `chmod` / 日志目录写入失败问题，改用 named volume 托管 `storage/` 与 `bootstrap/cache/`，并在 `app` 容器启动时初始化可写目录。
- 已验证：
  - `php artisan --version` 正常
  - MySQL PDO 连接正常
  - `http://localhost:7051` 返回 `200`

## 影响范围
- 后端代码目录：`app/backend/`
- 容器配置：`app/backend/docker-compose.yml`
- 阶段进度：`progress/阶段一/backend.md`

## 当前状态
- 后端本地开发环境已可用。
- 业务代码尚未开始，下一步进入 Schema、认证、RBAC 和后台管理 API 实现。
