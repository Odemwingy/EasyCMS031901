# 2026-03-23 Memory 7

- 新增角色详情接口 `GET /api/v1/admin/roles/{id}`。
- 新增角色编辑接口 `PUT /api/v1/admin/roles/{id}`。
- 角色详情返回已包含：
  - 基础信息
  - 数据范围说明
  - 已绑定模块/页面/操作权限
  - 原始权限标识列表
  - 已绑定用户列表
- 新增菜单管理 CRUD：
  - `POST /api/v1/admin/menus`
  - `PUT /api/v1/admin/menus/{id}`
  - `PATCH /api/v1/admin/menus/{id}/status`
  - `DELETE /api/v1/admin/menus/{id}`
- 菜单删除增加保护：
  - 有子节点时禁止删除
  - 已被角色绑定时禁止删除
- 菜单编辑增加父子级防环校验，避免把节点挂到自己或子孙节点下面。
- 已通过真实请求验证角色详情、角色编辑、菜单新增、菜单编辑、菜单停用、菜单删除整条链路。
