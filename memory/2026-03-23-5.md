# 2026-03-23 Memory 5

- 完成后台管理一期后端第二轮接口开发与联调。
- 已验证用户管理接口：列表、详情、创建、更新、状态切换、重置密码、解锁。
- 已补齐角色相关接口：列表、创建、复制、状态切换、权限读取、权限更新。
- 已新增菜单树接口 `GET /api/v1/admin/menus/tree`，返回前端权限配置页所需树结构。
- 用户列表接口已对齐前端分页口径：支持 `per_page`，返回 `per_page/current_page/last_page`。
- 用户与角色返回结构已补充标签字段，如 `status_label`、`user_type_label`。
- 角色权限保存时会自动补齐祖先菜单，避免权限页保存后导航菜单丢失。
- `UpdateUserRequest` 缺失 `Rule` 导入导致的 500 已修复。
- 修复了后端接口中的乱码提示文案，并为 API 异常增加统一 JSON 兜底。
- 种子数据已增加按钮级权限节点，便于权限配置页直接联调。
