# 记忆 2026-03-23-4

## 变更类型
后端功能开发

## 变更摘要
完成阶段一后端认证基础链路、核心数据迁移和默认管理员种子数据，后端已从“环境可用”推进到“认证可联调”。

## 变更详情
- 新增阶段一核心迁移：
  - `users` 表增强登录安全字段
  - `roles`
  - `menus`
  - `user_roles`
  - `user_projects`
  - `role_menu_permissions`
  - `audit_logs`
- 新增基础模型：
  - `Role`
  - `Menu`
  - `AuditLog`
  - 扩展 `User` 角色关系与认证字段
- 新增统一 API 响应结构，返回 `code / message / data / timestamp`
- 新增认证接口：
  - `POST /api/v1/auth/login`
  - `POST /api/v1/auth/logout`
  - `POST /api/v1/auth/change-password`
  - `GET /api/v1/auth/me`
  - `GET /api/v1/auth/menus`
- 登录链路已覆盖：
  - 用户名密码校验
  - 停用/锁定状态校验
  - 连续失败 5 次锁定
  - 30 分钟自动解锁
  - 登录成功签发 Sanctum Token
  - 登录成功/失败写入审计日志
- 默认种子数据已补：
  - 管理员账号：`admin / Abc12345`
  - 系统管理员角色
  - 后台管理一级菜单与用户/角色/菜单/审计日志菜单节点

## 影响范围
- 后端代码：`app/backend/app/`
- 路由：`app/backend/routes/api.php`
- 数据迁移：`app/backend/database/migrations/`
- 种子：`app/backend/database/seeders/DatabaseSeeder.php`
- 进度：`progress/阶段一/backend.md`、`progress/CURRENT_PROGRESS.md`

## 当前状态
- 后端认证链路可联调。
- 下一步进入用户管理、角色管理、菜单管理、审计日志接口实现。
