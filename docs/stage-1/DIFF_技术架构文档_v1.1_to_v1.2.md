# 技术架构文档 v1.1 → v1.2 差异对比

对比文件：

- 基线：`EasyCMS_技术架构文档_v1.1.md`
- 目标：`EasyCMS_技术架构文档_v1.2.md`

以下按 **增量**、**修改**、**删除** 三类归纳（基于全文 `diff` 结果）。

---

## 一、增量（新增内容）

| 位置 | 增量说明 |
|------|----------|
| **§4.1 前端目录结构** | `api/` 下增加示例文件：`media.ts`（注释说明：媒体管理元数据 CRUD，可选独立文件）。 |
| **§4.1 前端目录结构** | `features/` 下增加子目录：`media/`（媒体管理列表与表单）。 |
| **§5.8（新小节）** | **媒体管理模块（后台）** 完整约定：业务目标（元数据、不含直传/分片）、权限字表（`admin:media:list/create/edit/delete`）、REST 路径表（`/media-items` 系列）、前端 SPA 路由 `/media` 与菜单 `/admin/media` 映射、指向 §6.2 `media_items`。 |
| **§6.2 核心表设计** | SQL 示例块中 **新增 `media_items` 表**：字段（name、media_type、storage_path、mime_type、file_size、status、remark、created_by、时间戳及索引）。 |
| **§11.2 修订记录** | 新增一行 **v1.2**（2026-03-26）：媒体管理模块、§5.8、§6.2、`admin:media:*`、前后端 CRUD 与菜单种子同步说明。 |

**落地含意（待实现/已对齐代码时自检）**：若仓库已按 v1.2 实现，应存在：`media_items` 迁移、媒体 API、`/media` 页面、Seeder 中媒体菜单与权限；若仅有文档更新，则上述为**待办增量**。

---

## 二、修改（变更内容）

| 位置 | 变更说明 |
|------|----------|
| **文首元信息** | **版本**：`v1.1` → `v1.2`。 |
| **文首元信息** | **最后更新**：`2026-03-23` → `2026-03-26`。 |
| **文首排版** | 版本和日期行尾部增加空格以对齐 Markdown 换行（视觉/格式微调，无语义变化）。 |

除上述元信息外，**v1.1 既有章节正文未被替换或改写**；v1.2 是在保留 v1.1 全文基础上 **追加** §5.8、§6.2 表定义与修订记录。

---

## 三、删除（移除内容）

| 位置 | 删除说明 |
|------|----------|
| **正文** | **无大段删除**。diff 中未出现整节、整表或原则性条文的移除。 |
| **相对 v1.1** | 仅文首 **旧版本号/旧日期** 被新版本替代，属于「更新」而非「删除章节」。 |

结论：**v1.2 相对 v1.1 为向后兼容的扩展版**，删除项可记为 **无**；合并时只需关注 **新增 §5.8、§6.2 媒体表、§4.1 两行目录示例及修订记录**，以及 **更新文档版本与日期**。

---

## 四、执行清单（从文档差异到工程）

若目标为「文档与实现一致」，建议按增量逐项核对：

1. **数据库**：存在 `media_items` 表及迁移。  
2. **后端**：`/api/v1/admin/media-items` CRUD + `admin:media:*` 中间件；菜单/种子含媒体项。  
3. **前端**：`/media`（或文档所述路径）、API 封装、侧栏/权限路由白名单。  
4. **接口文档**：是否在 `EasyCMS_接口文档_后台管理_v1.0.md` 中同步增补媒体章节（当前 diff 未包含该文件，可按项目流程另开文档修订）。

---

*本对比由 v1.1 与 v1.2 文件 diff 生成，若后续任一侧有手工改动，请重新运行 diff 更新本文件。*
