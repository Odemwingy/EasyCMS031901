[pytest]
addopts = -q -ra --strict-markers
python_files = test_*.py
python_classes = Test*
python_functions = test_*
testpaths = tests
markers =
    case_ids(*ids): 关联测试用例编号
    bug_side(side): 缺陷归属，通常为前端或后端
